<p id="notice"><%= notice %></p>

<h1>Movie's List</h1>

<table>
  <thead>
    <tr>
      <th>Name 🍴</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td><%= list.name %></td>
        <td><%= list.category %></td>
        <td><%= list.address %></td>
        <td><%= link_to 'Show', list %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Create a new List', new_list_path %>
